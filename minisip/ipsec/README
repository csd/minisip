
To use MsipIpsecAPI specify

--enable-ipsec-enable 

when running ./configure


In the .minisip.conf file specify

<use_ipsec>
        yes
</use_ipsec>

Note that if minisip is ipsec enabled it will always accept ipsec calls even if use_ipsec = no.

if use_ipsec = yes minisip will make ipsec calls and reject incoming calls that are not ipsec.
