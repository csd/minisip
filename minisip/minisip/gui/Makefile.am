SUBDIRS = 
DIST_SUBDIRS = textui gtkgui qtgui openglgui confgui 


if TEXT_UI
        SUBDIRS += textui
endif

if GTK_GUI
        SUBDIRS += gtkgui
endif

if OPENGL_GUI
        SUBDIRS += openglgui
endif

if QT_GUI
        SUBDIRS += qtgui
endif

if CONF_GUI
        SUBDIRS += confgui
endif


MAINTAINERCLEANFILES = $(srcdir)/Makefile.in
