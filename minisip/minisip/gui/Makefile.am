SUBDIRS = 
DIST_SUBDIRS = textui gtkgui qtgui confgui 


if TEXT_UI
        SUBDIRS += textui
endif

if GTK_GUI
        SUBDIRS += gtkgui
endif

if QT_GUI
        SUBDIRS += qtgui
endif

if CONF_GUI
        SUBDIRS += confgui
endif


MAINTAINERCLEANFILES = $(srcdir)/Makefile.in
